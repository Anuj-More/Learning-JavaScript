//array = Just a variable which can store multiple values 

let fruits = ["apple", "mango", "banana"];

fruits.push("guava");      //adds elements to the end
fruits.pop();              //removes an element from the end
fruits.unshift("chikoo");  //adds element in the beginning
fruits.shift();            //removes element from the beginning  
// console.log(fruits);

let length = fruits.length;
// console.log(length);

let index = fruits.indexOf("kiwi");
// console.log(index);
